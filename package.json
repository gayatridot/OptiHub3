{
  "name": "optihub3",
  "version": "1.0.0",
  "description": "Audio & Video Processing Hub — Beta Version",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "ISC",
  "dependencies": {
    "lamejs": "^1.2.1"
  }
}
